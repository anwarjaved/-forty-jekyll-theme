{
  "name": "minimal-mistakes",
  "private": true,
  "version": "4.24.0",
  "description": "Minimal Mistakes 2 column Jekyll theme.",
  "repository": {
    "type": "git",
    "url": "git://github.com/mmistakes/minimal-mistakes.git"
  },
  "keywords": [
    "jekyll",
    "theme",
    "minimal"
  ],
  "author": "Michael Rose",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/mmistakes/minimal-mistakes/issues"
  },
  "homepage": "https://mmistakes.github.io/minimal-mistakes/",
  "engines": {
    "node": ">= 0.10.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "onchange": "^7.1.0",
    "uglify-js": "^3.13.6"
  },
  "scripts": {
    "dev": "tinacms dev -c \"bundle exec jekyll serve -d build\"",
    "build": "tinacms build && bundle exec jekyll build  -d build"
  },
  "dependencies": {
    "@tinacms/cli": "^1.4.0",
    "tinacms": "^1.4.0"
  }
}
